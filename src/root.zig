const core = @import("core");
const rt = @import("runtime");

pub const App = rt.App;
pub const Window = rt.Window;
pub const Platform = rt.Platform;
pub const Monitor = rt.Monitor;
pub const Keyboard = rt.Keyboard;
// pub const Event = rt.Event;
// pub const Keyboard = rt.Keyboard;

// pub const app = runtime.app;
// pub const window = runtime.window;
// pub const event = runtime.event;
// pub const monitor = runtime.monitor;

// pub const WindowHandle = runtime.WindowHandle;
// pub const MonitorHandle = runtime.MonitorHandle;
// pub const KeyboardHandle = runtime.KeyboardHandle;

// pub const InputDeviceId = core.InputDeviceId;
// pub const KeyboardId = core.KeyboardId;
